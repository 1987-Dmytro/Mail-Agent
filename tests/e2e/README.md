# E2E (End-to-End) Tests for Mail Agent

## 🎯 Назначение

E2E тесты проверяют **РЕАЛЬНУЮ интеграцию** со всеми внешними сервисами:
- **Gmail API** - реальные операции с почтой
- **Gemini API** - реальная AI классификация (стоит денег!)
- **Telegram Bot API** - реальные сообщения в Telegram
- **PostgreSQL** - реальная база данных
- **LangGraph** - реальные workflow

**⚠️ ВАЖНО:** Эти тесты делают РЕАЛЬНЫЕ API вызовы, которые:
- Стоят денег (Gemini API)
- Отправляют реальные сообщения в Telegram
- Создают/удаляют реальные Gmail labels
- Занимают 30-60 секунд на выполнение

**Запускайте E2E тесты только:**
- Перед релизом в production
- После критических изменений в интеграциях
- Вручную, когда нужно убедиться что всё работает

---

## 📋 Список E2E тестов

### 1. `test_gmail_real_api.py` - Gmail API Integration
- ✅ Создание и удаление Gmail labels
- ✅ Применение labels к письмам
- ✅ OAuth token refresh
- **Требует:** `GMAIL_TEST_OAUTH_TOKEN`, `GMAIL_TEST_REFRESH_TOKEN`

### 2. `test_telegram_real_api.py` - Telegram Bot API Integration
- ✅ Отправка простых сообщений
- ✅ Отправка сообщений с кнопками (inline keyboard)
- ✅ Редактирование сообщений
- ✅ HTML форматирование
- **Требует:** `TELEGRAM_BOT_TOKEN`, `TELEGRAM_TEST_CHAT_ID`

### 3. `test_complete_workflow_e2e.py` - КРИТИЧНЫЙ! Complete Workflow
- 🚨 **САМЫЙ ВАЖНЫЙ ТЕСТ!**
- ✅ Полный цикл: Gmail → Gemini AI → Telegram → Database
- ✅ Проверяет ВСЕ интеграции вместе
- **Требует:** ВСЕ environment variables

---

## 🛠️ Настройка окружения

### Шаг 1: Получение учетных данных

#### Gmail API (требуется для Gmail тестов)

1. **Создать OAuth credentials:**
   ```bash
   # Используйте существующий OAuth flow из приложения
   # После успешной авторизации получите токены
   ```

2. **Получить токены из базы данных:**
   ```bash
   # Подключитесь к dev базе данных
   psql $DATABASE_URL

   # Получите токены вашего тестового пользователя
   SELECT gmail_oauth_token, gmail_refresh_token FROM users WHERE email = 'your_test@gmail.com';
   ```

3. **Скопировать токены:**
   ```bash
   # Расшифруйте токены (если они зашифрованы)
   # Сохраните для использования в environment variables
   ```

#### Telegram Bot (требуется для Telegram тестов)

1. **Получить bot token:**
   - Напишите [@BotFather](https://t.me/BotFather) в Telegram
   - Создайте нового бота: `/newbot`
   - Скопируйте token (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

2. **Получить ваш Telegram chat ID:**
   ```bash
   # Запустите бота, отправьте ему /start
   # Затем используйте API для получения chat_id:
   curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates

   # В ответе найдите: "chat":{"id":123456789,...}
   # Это ваш TELEGRAM_TEST_CHAT_ID
   ```

#### Gemini API (требуется для AI тестов)

1. **Получить API key:**
   - Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Создайте новый API key
   - ⚠️ Этот ключ будет использоваться для ПЛАТНЫХ API вызовов!

### Шаг 2: Настройка environment variables

Создайте файл `.env.e2e` в корне проекта:

```bash
# Gmail API credentials
GMAIL_TEST_OAUTH_TOKEN="ya29.a0AfH6SMBxxxxxxxxxxxxx"
GMAIL_TEST_REFRESH_TOKEN="1//0gxxxxxxxxxxxxx"
GMAIL_TEST_EMAIL="your_test_account@gmail.com"

# Telegram Bot credentials
TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
TELEGRAM_TEST_CHAT_ID="123456789"

# Gemini API (⚠️ стоит денег!)
GEMINI_API_KEY="AIzaSyxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Database (можно использовать dev базу)
DATABASE_URL="postgresql+psycopg://mailagent:mailagent_dev_password_2024@localhost:5432/mailagent"
```

**⚠️ ВАЖНО:**
- НЕ коммитьте `.env.e2e` в git!
- Добавьте в `.gitignore`: `*.env.e2e`

### Шаг 3: Загрузка environment variables

```bash
# Загрузить переменные из файла
export $(cat .env.e2e | xargs)

# Или использовать direnv (рекомендуется)
# Установите direnv: https://direnv.net/
# Создайте .envrc:
echo 'export $(cat .env.e2e | xargs)' > .envrc
direnv allow
```

---

## 🚀 Запуск E2E тестов

### Запустить все E2E тесты

```bash
# ⚠️ ВНИМАНИЕ: Это сделает РЕАЛЬНЫЕ API вызовы!
pytest tests/e2e/ -v -m e2e --tb=short
```

### Запустить конкретный тест

```bash
# Только Gmail API тесты
pytest tests/e2e/test_gmail_real_api.py -v -m e2e

# Только Telegram API тесты
pytest tests/e2e/test_telegram_real_api.py -v -m e2e

# 🚨 КРИТИЧНЫЙ: Complete workflow (СТОИТ ДЕНЕГ!)
pytest tests/e2e/test_complete_workflow_e2e.py -v -m e2e
```

### Запустить конкретный тест-кейс

```bash
# Только один тест
pytest tests/e2e/test_gmail_real_api.py::TestGmailRealAPI::test_gmail_create_and_delete_label_e2e -v

# Complete workflow с детальным выводом
pytest tests/e2e/test_complete_workflow_e2e.py::TestCompleteWorkflowE2E::test_complete_email_classification_flow_e2e -v -s
```

### Запустить с подробным логированием

```bash
# С полным traceback и print outputs
pytest tests/e2e/ -v -m e2e -s --tb=long

# С покрытием кода
pytest tests/e2e/ -v -m e2e --cov=app --cov-report=html
```

---

## ✅ Что проверять после запуска

### Gmail API тесты:
1. ✅ Тесты проходят без ошибок
2. ✅ Gmail labels создаются и удаляются
3. ✅ Labels применяются к письмам корректно

### Telegram API тесты:
1. ✅ Тесты проходят без ошибок
2. **📱 ПРОВЕРЬТЕ TELEGRAM APP:**
   - Вы должны получить тестовые сообщения
   - Кнопки должны отображаться корректно
   - Форматирование (bold, italic) должно работать

### Complete Workflow тест:
1. ✅ Тест проходит без ошибок
2. **📱 ПРОВЕРЬТЕ TELEGRAM APP:**
   - Вы должны получить уведомление с AI классификацией
3. ✅ Проверьте вывод теста - должны быть checkmarks:
   ```
   ✅ Verified:
      ✅ Gmail API: Fetched real email
      ✅ Gemini AI: Classified email with real AI
      ✅ Telegram Bot: Sent real notification
      ✅ Database: Persisted data correctly
      ✅ Integration: All services work together
   ```

---

## 🐛 Troubleshooting

### Ошибка: "GMAIL_TEST_OAUTH_TOKEN not set"
**Решение:** Убедитесь что environment variables загружены:
```bash
echo $GMAIL_TEST_OAUTH_TOKEN
# Должен вывести токен, не пустую строку
```

### Ошибка: "No messages in test Gmail account"
**Решение:** Отправьте хотя бы 1 email на тестовый Gmail аккаунт

### Ошибка: Telegram API "Unauthorized"
**Решение:** Проверьте bot token:
```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
# Должен вернуть информацию о боте
```

### Ошибка: "Chat not found"
**Решение:**
1. Запустите бота в Telegram (отправьте /start)
2. Убедитесь что `TELEGRAM_TEST_CHAT_ID` правильный

### Gemini API Error: "API key invalid"
**Решение:** Проверьте API key:
```bash
curl \
  -H 'Content-Type: application/json' \
  -d '{"contents":[{"parts":[{"text":"Hello"}]}]}' \
  "https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=$GEMINI_API_KEY"
```

### Tests очень медленные
**Это нормально!** E2E тесты с реальными API делают настоящие HTTP запросы:
- Gmail API: 2-5 сек на запрос
- Gemini AI: 3-10 сек на классификацию
- Telegram API: 1-2 сек на сообщение

**Итого:** Complete workflow тест занимает 30-60 секунд.

---

## 💰 Стоимость запуска E2E тестов

### Gmail API
- **Бесплатно** до 250 quota units/день
- Наши тесты используют ~5-10 units
- ✅ **Практически бесплатно**

### Telegram Bot API
- **Полностью бесплатно**
- Без лимитов для ботов
- ✅ **Бесплатно**

### Gemini API
- **ПЛАТНЫЙ!** ~$0.001-0.002 за 1000 tokens
- Один complete workflow тест: ~500-1000 tokens
- **Стоимость 1 запуска:** ~$0.001-0.002 (меньше цента)
- **10 запусков:** ~$0.01-0.02
- ⚠️ **Стоит денег, но очень дешево**

**Итого:** Запуск всех E2E тестов стоит **~$0.01-0.03** (1-3 цента)

---

## 📊 CI/CD Integration (будущее - Epic 4)

В Epic 4 мы настроим:
- ✅ E2E тесты в отдельном GitHub Actions workflow
- ✅ Manual trigger (запуск по кнопке, не автоматически)
- ✅ Secrets для API keys в GitHub
- ✅ Slack notifications о результатах
- ✅ Pre-production staging environment

**Сейчас:** Запускайте E2E тесты локально вручную перед релизами.

---

## 🎯 Когда запускать E2E тесты

### ✅ ОБЯЗАТЕЛЬНО запускать:
- **Перед релизом в production**
- После изменений в Gmail integration
- После изменений в Telegram bot
- После изменений в AI classification
- После обновления зависимостей (major versions)

### ⚠️ ОПЦИОНАЛЬНО запускать:
- После bugfixes в интеграциях
- Перед демо для клиента
- Когда хотите убедиться что всё работает

### ❌ НЕ запускать:
- В CI/CD на каждый commit (используйте mock tests)
- Во время разработки (используйте integration tests)
- Часто (экономьте деньги на Gemini API)

---

## 📝 Notes

1. **E2E тесты НЕ заменяют integration tests!**
   - Integration tests (с моками) - быстрые, бесплатные, запускаются всегда
   - E2E tests (с реальными API) - медленные, платные, запускаются редко

2. **Почему мы используем моки в большинстве тестов?**
   - Gmail API: Rate limits (250/день)
   - Telegram API: Можно получить ban за спам
   - Gemini API: Стоит денег
   - Скорость: Тесты с моками выполняются за секунды, не минуты

3. **E2E тесты - это "smoke tests" для production**
   - Они проверяют что критичные интеграции работают
   - Они НЕ заменяют полное тестирование
   - Они дают уверенность перед релизом

---

## 🤝 Contributing

При добавлении новых E2E тестов:

1. **Обязательно:**
   - Используйте `@pytest.mark.e2e` marker
   - Добавьте `@pytest.mark.skipif` с проверкой env variables
   - Добавьте cleanup в `finally` блоки
   - Документируйте что тест проверяет

2. **Рекомендуется:**
   - Выводите понятные сообщения с checkmarks (✅)
   - Указывайте в docstring стоимость запуска (если Gemini API)
   - Добавляйте примеры в этот README

3. **Избегайте:**
   - Слишком много E2E тестов (они дорогие)
   - Дублирования integration tests
   - Тестов без cleanup (оставляют мусор в Gmail/Telegram)

---

## 📚 Дополнительные ресурсы

- [Gmail API Documentation](https://developers.google.com/gmail/api)
- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [Gemini API Documentation](https://ai.google.dev/docs)
- [pytest Documentation](https://docs.pytest.org/)
- [pytest-asyncio Documentation](https://pytest-asyncio.readthedocs.io/)

---

**Последнее обновление:** 2025-11-08
**Версия:** Epic 2 - Initial E2E Test Suite
