
# Mark problematic e2e tests for isolated execution
[pytest]
markers =
    isolated: mark test to run in isolation (deselect with '-m "not isolated"')
    e2e: mark test as end-to-end test requiring real API credentials
    integration: mark test as integration test (uses real services/mocks)
    slow: mark test as slow running (typically >1 second)
